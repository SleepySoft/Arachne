我想建立一个名为Arachne的系统与数据库，用以建立世界实体和事件的联系，然后通过在不同关系间爬行，找到潜在的规律或提前预知从而使自己处于有利地位。

我的草稿设想如下：

+ 实体
实体分为：人（Person），物（Thing），组织（Organization），地点（Place），概念（Concept），指标（Metric）
每一类实体有固定字段和任意可扩充字段
注意事件不在其中，我的IIS用来记录事件和分解实体，通过实体关联在Arachne中爬行
事件可以在推演时作为临时结点和边，但不保存入数据库
实体是否需要一张专门的表？

+ 关系
关系需要分类
每类关系可独立显示，也可以叠加及混合显示，每类关系是一个图层
关系毫无疑问可以体现在不同实体之间，比如从人爬行到组织
关系爬行也毫无疑问可以跨关系图层，比如从“影响”到“上下游”
关系的分类需要好好设计
每类关系是否使用单独的表？

+ 因果
有一类特殊的关系叫因果，或：可能影响、经验，比如：
	伊朗被打->霍尔木兹海峡封锁->油价上升，然后系统再通过油这种“物”的实体去进一步发掘会涨价的其它“物”
	疫情->提前囤积物资，特别是口罩
	诸如此类
因果通常由人发掘，也可以使用特殊方式让LLM发掘：“A和B有相关性，如果非A，是不是依然可能B？或者如果非B，是否可能A？”

请帮我头脑风暴和推演，给出最符合客观世界、数学和哲学的设计。








我们考虑一个场景：研究上市公司时，建立公司的控股体系，创建并关联产品，以及其客户关系

这是一个很自然简单的想法，但细想起来里面很多细节我还是想不通。









当前批次：NNN = 004




本电脑已通过环境变量“TUSHARE_TOKEN”配置了TUSHARE的token。

for NNN from 51 to 55:
    使用tushare，获取 data/stock_batches/batch_NNN.json 中各家公司的股票信息。
    对每一个股票，考察其公司的原材料（输入）和产出，分析和构建产业图，同时构建对应的公司视图。

    在构建过程中，原则上除了发生程序上的错误，原则上不得修改代码。
    在操作前先阅读 backend/app/models 理解模型和设计。

    构建过程需符合本系统设计的原则：
    1. 产业实体图仅表达实体和服务之间的关系，图中不得包含公司、产业、人员、组织
    2. 行业是配置出来的节点过滤器，它仅是一个选择器和视图，不是图中的元素，它独立存储在一个数据库中
    3. 公司和行业同理，它不是图中的元素，它也独立存储于一个数据库中

    要求：
    1. 所有操作都要通过系统API提交（包括通过cli提交）
    2. 新增任何实体前，先查看已有实体和关系，从而确定添加或更新策略
    3. 临时生成的注入内容脚本命名为 tmp_script/tmp_submit_batch_NNN.py （NNN为当前处理的批次号）
    4. 对于每一个公司和实体的数据，都要经过你分析和亲自构造提交，不允许脚本直接批量添加。
    5. 对于不确定的信息和实体关系，需要通过网络核查。
    6. 对于缺失的产业物料和服务节点，应该进行网络查询与核实，并向数据库中补录高质量的节点信息。

    仔细规划该任务。在执行过程中将过程、发现和启发记录到文件：docs/batch_NNN_construction_log.md









我希望在用户点击子图节点时能临时显示其关联的，不在本子图中的结点，双击固定显示，且考虑一路单击最后双击的情况。









我有大量的token，可以通过OPENAI风格的API调用，我想使用这个资源为我的项目准备数据。
这个项目叫Arachne，是一个图谱项目，当前我的目标是产业物料和服务的上下游关系梳理，以及公司的相关的物料（产出，最好有依赖，没有可以通过产业物料图推导），以及基本信息、关系信息。
所有这些信息并不需要直接注入系统中，而是生成结构化json文件，这个文件可以复用和再加工。

我面对的问题在于：
1. 信息的抽取要全面，且尽量贴近Arachne系统的格式，暂时用不到的信息预留即可。
2. 物料/服务构成的产业图是核心，但物料/服务很难凭空想出来，所以以公司的产品为契机进行添加是最合理的想法。
3. 原始信息会提到物料，不可能告诉你物料的上下游关系，且物料本身也存在多种概念，如何清晰地合并和归类这些概念，这是对AI的要求。
4. 基于以上理由，仅通过API调用是不够的，我需要使用agent。
5. agent需要支持通用AI服务，且可被脚本控制（脚本主循环读取股票列表，一个个要求agent分析，防止agent偷懒）

我暂时想到这么多。由于这个工作资源消耗巨大，所以我希望充分规划后执行。请说说你的看法。






















接下来我想建立人和公司的图谱，这个图谱和产业图谱不在一张表中，并且和公司视图的设计不冲突。它是一张独立图，用来保存公司和人的事实关联，由年报和天眼查等渠道采集汇总。
公司数据跟之前一样可以通过与产业图谱计算得到公司视图，也能通过探索的方式，从两张图中不断发掘下一个节点。
你是否能明白我的设计？公司关联的schema已经有了（company_view_schema.py），需要review是否合适，是否需要扩充（除了持股关联，还有业务上下游之类的产业事实关联）。
同时设计人员字段，注意人和人之间也有关系，人是独立的个体，并不需要依附于公司。



我们接下来执行以下工作流：

1. 查找并整理芯片行业的所有A股上市公司和国外主要公司。
2. 对于每个公司，确定它们在产业上下游的位置。
3. 确认每个公司的产品和能力。
4. 按产业上下游分类列出公司，并列出对应公司产品到一个文档，作为研报，供我阅读。
5. 录入公司和产品信息。
6. 对于录入节点，搜索并建立或关联正确的关系（特别是上下游）。













TODO:
    Rules:
        如果一个节点是别名节点（alias_of），则该节点不能关联其它节点，即必须通过标准名关联其它节点


    右键菜单中“显示/高亮/拉近”作为一级菜单，“上游/下游”作为二级菜单，减少菜单长度。


    创建连线窗口超出屏幕，显示不完整。












我的系统设计中，图暂时有三类：产业图，关系图，概念图。

产业图即你看到的schema所描述的图。
关系图是企业、组织、人之间的关联。
概念图描述经验、因果，等等。

不同类型的图之间不能有边连接，其索引依靠pg中的metadata，为加速推理可以生成层间互连的临时图，但不可直接加入正式图。

至于地域等，还没考虑好是归于一类还是作为新的一类或通用类。

关于你的描述，对我启发最大的在于以下几点：

1. 推理引擎分类的说法
2. 推理输出必须包含事实引用
3. 情报能发现新的物料与产业关联
4. 边增加上游物料在该流程中的角色等辅助推理的信息
5. 这个程序不预测未来

至于情报采集、分析、提取，我有IIS系统在做，因此不需要包含到本系统中。本系统只需要根据调用者传入的物料和实体进行推理即可。
推理结果可以生成临时图，但不能进主图。







